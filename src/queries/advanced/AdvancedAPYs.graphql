query AdvancedAPYs($season_gte: Int, $first: Int, $token: Bytes) {
  seasons: tokenYields(
    where: {season_gte: $season_gte, token: $token, siloYield_: {emaWindow: ROLLING_30_DAY}}
    first: $first
    orderBy: season
    orderDirection: desc
  ) {
    season
    beanAPY
    createdAt
  }
}